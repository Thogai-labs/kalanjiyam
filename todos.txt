[4/5] STEP 6/10: RUN python /app/kalanjiyam/scripts/fetch_i18n_files.py &&     pybabel compile -d /app/kalanjiyam/translations -f ||     echo "WARNING: i18n fetch failed, running in English only"
Cloning into '/app/data/kalanjiyam-i18n'...
fatal: could not read Username for 'https://github.com': No such device or address
WARNING: could not clone https://github.com/AnaadiAI/kalanjiyam-i18n.git (private repo or network). App will run in English only.
Traceback (most recent call last):
  File "/app/.venv/bin/pybabel", line 10, in <module>
    sys.exit(main())
             ^^^^^^
  File "/app/.venv/lib/python3.11/site-packages/babel/messages/frontend.py", line 1050, in main
    return CommandLineInterface().run(sys.argv)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/app/.venv/lib/python3.11/site-packages/babel/messages/frontend.py", line 976, in run
    return cmdinst.run()
           ^^^^^^^^^^^^^
  File "/app/.venv/lib/python3.11/site-packages/babel/messages/frontend.py", line 199, in run
    for errors in self._run_domain(domain).values():
                  ^^^^^^^^^^^^^^^^^^^^^^^^
  File "/app/.venv/lib/python3.11/site-packages/babel/messages/frontend.py", line 230, in _run_domain
    raise OptionError(f'no message catalogs found for domain {domain!r}')
babel.messages.frontend.OptionError: no message catalogs found for domain 'messages'
WARNING: i18n fetch failed, running in English only

# ==============================================================================
# TODO: CLI & UI Batch OCR Admin View & CSV Export Feature [COMPLETED]
# ==============================================================================
# 1. SQL Query for Batch OCR metrics:
#    docker exec -it kalanjiyam-db psql -U kalanjiyam -d kalanjiyam -c \
#    "SELECT b.file_path AS name, pg_size_pretty(b.source_size_bytes::bigint) AS size, COALESCE(b.total_pages, page_counts.cnt, 0) AS pages, ROUND((b.total_ocr_latency_ms / 1000)::numeric, 2) || 's' AS time_took_to_ocr, b.status FROM batch_items b LEFT JOIN (SELECT batch_item_id, COUNT(*) as cnt FROM batch_ocr_pages GROUP BY batch_item_id) page_counts ON page_counts.batch_item_id = b.id ORDER BY b.id DESC;"
#
# 2. [x] #implement superadmin & admin view for CLI / UI batch OCR, UI batch translation, and Single Page Proofing OCR/Translation:
#    - [x] Renamed sidebar item to `OCR & TR Metrics` with a Speedometer (gauge) icon for Superadmin and Org Admin views.
#    - [x] Separate UI pages for Batch Jobs List (Page 1) and Job Metrics Details (Page 2). Removed `Target URI / Path` column from Jobs List table.
#    - [x] Added Sub-Nav Category Tabs (`All Jobs`, `Proofer Single Page`, `UI Batch`, `CLI Batch`).
#    - [x] Added Task Type Filter (`All Types`, `OCR Only`, `Translation Only`) and real-time URI / Job Type search input.
#    - [x] Add average per-page latency metrics calculated by (time_took / pages).
#    - [x] Group single-page proofing metrics (`SINGLE_PAGE_PROOFING_OCR` & `SINGLE_PAGE_PROOFING_TRANSLATION`) by Organization and Document Slug so document summary and per-page CSVs contain accurate document data.
#    - [x] Track and display document-level & per-page metrics for CLI/UI batch OCR, UI batch translation, and single page proofing (File Name, Source Lang, Target Lang, Translation Time Took, Translation Data Size, Extracted Image size, OCR time took, OCR text/JSON data size, Cropped elements image size).
#    - [x] Add separate functions/endpoints to export Document Summary CSV and Per-Page Breakdown CSV according to job ID.